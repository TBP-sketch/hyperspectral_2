# 参赛演示脚本建议

演示时建议按以下 **3 个关键步骤** 展示软件功能，总时长约 5–8 分钟。

---

## 步骤一：数据加载与多格式支持（约 2 分钟）

1. 打开软件，简要介绍界面：左侧四模块导航（数据读取 / 预处理 / 导出 / 可视化）。
2. 点击 **「📥 数据读取」**，通过「浏览」选择一份高光谱数据（建议提前准备：`.npy`、`.hdr` 或 `.h5` 任选其一）。
3. 加载成功后，指出状态栏中的 **数据形状 (H, W, B)**，并说明支持 **ENVI、HDF5、NumPy、RAW** 等多种格式。
4. （可选）演示 **「数据格式转换为 ENVI」** 入口，说明可将其他格式统一转为 ENVI 便于后续处理。

**要点**：突出多源数据接入与格式兼容性。

---

## 步骤二：预处理与可视化联动（约 2–3 分钟）

1. 切换到 **「⚙️ 预处理」**，演示 **辐射定标**：勾选「启用自动转换」或输入增益/偏移，点击「应用定标」。
2. 说明定标后数据会 **自动同步** 到其他模块。
3. 切换到 **「📊 可视化」**：
   - 选择不同 **波段** 查看单波段灰度图，调节 **对比度拉伸** 滑块；
   - 在 **假彩色合成** 中选择 R/G/B 波段，点击「生成假彩色图」展示彩色合成；
   - 在图像上 **左键点击** 某像素，右侧出现该点 **光谱曲线**（可指出标题中的坐标与 min/max 值）。
4. （可选）演示 **滚轮缩放**、**中/右键拖拽平移** 或 **清除曲线 / 保存图像** 等交互。

**要点**：强调预处理结果实时反馈到可视化，以及单波段/假彩色/光谱曲线的一体化展示。

---

## 步骤三：导出与收尾（约 1–2 分钟）

1. 切换到 **「💾 导出」**，选择导出格式（如 **ENVI** 或 **CSV**），选择范围（全图或当前像素光谱），点击「浏览」选择保存路径。
2. 点击 **「开始导出」**，等待进度与日志完成，弹出「导出完成」提示。
3. 收尾时可快速展示：
   - **快捷键**：Ctrl+O 打开、F1 帮助；
   - **视图 → 深色模式** 切换主题；
   - **帮助 → 关于** 显示软件名称、版本与技术栈。

**要点**：体现从数据加载、预处理、可视读到导出的一站式流程，以及快捷键与主题等细节体验。

---

## 演示前检查清单

- [ ] 准备 1–2 个示例数据文件（.npy / .hdr / .h5）放在桌面或固定目录。
- [ ] 确认分辨率与投影（如 1920×1080），避免窗口被遮挡。
- [ ] 如需展示大气校正，可提前说明「执行大气校正」会后台运行并更新数据与可视化。
